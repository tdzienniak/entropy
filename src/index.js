(function (app) {
    "use strict";

    function Index (dimension) {
        this._index = [];

        this.dimension = dimension;
    }

    var p = Index.prototype;

    p.set = function () {

    };

    p.unset = function () {

    };

    p.clear = function () {
        
    }

})(app);